#### 这个爬虫与我仓库中的另个一个爬虫一样，

#### 只是数据库不同，以前是写入一个Typecho的sqlite的数据库

#### 这次则是Wordpress的MySQL数据库，而且数据库设计的有点复杂，

#### 自己琢磨了很长时间也没搞清楚某些字段的具体作用。

#### 虽然能爬取文章并写入数据库，包括文章对应的分类，标签。

#### 但依然有不尽人意处，没有找到文章缩略图存储的地方。

#### 所以把文章的改为审核，这样可以自己在后台修改后发表

#### 也可以对文章把把关。就先这样吧。

